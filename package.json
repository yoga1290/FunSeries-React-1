{
  "name": "react1",
  "version": "1.0.0",
  "description": "React is a JavaScript library for building user interfaces.",
  "main": "main.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
  	"start": "watchify -t babelify main.js -o bundle.js -v",
    "browserify": "browserify -t babelify main.js -o bundle.js",
    "watchify": "watchify -t babelify main.js -o bundle.js -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "hmehta",
  "license": "ISC",
  "dependencies": {
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "react": "^15.0.2",
    "react-dom": "^15.0.2"
  },
  "devDependencies": {
    "watchify": "^3.7.0"
  }
}
